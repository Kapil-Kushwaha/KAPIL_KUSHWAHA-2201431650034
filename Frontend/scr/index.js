import React from 'react';
import ReactDOM from 'react-dom/client';
import App from 'Kapil(2201431650034)/Frontend/scr/App';
import './styles/global.css'; // Optional: Only if you're using global CSS

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);